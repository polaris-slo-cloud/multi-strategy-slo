{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/resource-observer/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/node:webpack",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/resource-observer",
        "main": "apps/resource-observer/src/main.ts",
        "tsConfig": "apps/resource-observer/tsconfig.app.json",
        "assets": ["apps/resource-observer/src/assets"],
        "externalDependencies": "none"
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false,
          "fileReplacements": [
            {
              "replace": "apps/resource-observer/src/environments/environment.ts",
              "with": "apps/resource-observer/src/environments/environment.prod.ts"
            }
          ]
        }
      }
    },
    "serve": {
      "executor": "@nrwl/node:node",
      "options": {
        "buildTarget": "resource-observer:build"
      },
      "configurations": {
        "production": {
          "buildTarget": "resource-observer:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/resource-observer/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/resource-observer"],
      "options": {
        "jestConfig": "apps/resource-observer/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "docker build -f ./apps/resource-observer/Dockerfile --build-arg POLARIS_APP_TYPE=slo --build-arg POLARIS_APP_NAME=resource-observer -t stvnkiss/resource-observer:latest ."
        ],
        "parallel": false
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "kubectl apply -f ./apps/resource-observer/manifests/kubernetes"
        ],
        "parallel": false
      }
    }
  },
  "tags": []
}
